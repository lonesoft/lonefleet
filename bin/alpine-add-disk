#!/usr/bin/env bash

set -euo pipefail

function main(){
  local volume_name=${1:-sdb}
  local volume_mount=${2:-/mnt/data}
  VOLUME_NAME=$volume_name \
    VOLUME_MOUNT=$volume_mount \
    deploy deploy fleet/alpine/alpine-add-disk
}

main "$@"
