#!/usr/bin/env bash

set -euo pipefail

function main(){
  local volume_name=${1:-/dev/sdb}
  local volume_mount=${2:-/mnt/data}
  deploy env VOLUME_NAME=$volume_name \
          +env VOLUME_MOUNT=$volume_mount \
          +file fleet/alpine/bin/add-disk
}

main "$@"
