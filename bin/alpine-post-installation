#!/usr/bin/env bash

set -euo pipefail

function main(){
  [[ ! -z $PROJECT_PATH ]] && cd $PROJECT_PATH
  . dotenv ""
  alpine-reset-known-hosts
  deploy root-file fleet/alpine/bin/post-installation
}

main "$@"
